// definir as config. iniciais do programa no setup
void setup(){
    // definir o pino 13 como saída de informação
    // pinMode(pino, entrada ou saída)
    pinMode(13, OUTPUT);
    // ligar o pino 13
    // definir o valor de uma saída digital
    // nas saídas - escrever - Write
    // digitalWrite(pino, valor digital (0 ou 1))
    digitalWrite(13, 1);
}

void loop(){}